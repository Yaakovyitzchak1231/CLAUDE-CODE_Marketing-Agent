# Core LangChain and LangGraph
# Updated for CrewAI compatibility (requires langchain >= 0.2)
langchain>=0.2.0,<0.4.0
langchain-community>=0.2.0,<0.4.0
langchain-core>=0.2.0,<0.4.0
langgraph>=0.1.0,<1.0.0
langsmith>=0.1.40

# LLM Providers
openai>=1.58.1  # For DALL-E 3, compatible with httpx 0.27+
langchain-openai>=0.1.6,<0.3.0  # For OpenAI LLM integration with LangChain

# Vector Store
chromadb==0.5.23
sentence-transformers==3.3.1  # Updated for huggingface_hub compatibility

# Database
psycopg2-binary==2.9.9
redis==5.0.1

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.25.0
pydantic==2.5.3
pydantic-settings==2.1.0

# HTTP & API Clients
requests==2.31.0
httpx==0.27.2
aiohttp==3.9.1

# Web Scraping
beautifulsoup4==4.12.3
playwright==1.40.0
scrapy==2.11.0
selenium==4.16.0
trafilatura==1.6.3              # Fast web content extraction

# Video Processing
ffmpeg-python==0.2.0

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.6
pyyaml==6.0.1
jsonschema==4.20.0

# Market Research & Trends
pytrends==4.9.2  # Google Trends API
lxml==5.1.0  # XML parsing for SEC/government data

# =============================================================================
# NLP & TEXT ANALYSIS - CRITICAL FOR BRAND VOICE (NOT OPTIONAL)
# =============================================================================
# These libraries ensure content does NOT sound like AI

# Core NLP Libraries - REQUIRED
spacy==3.7.4                    # Advanced NLP, linguistic analysis, entity extraction
textstat==0.7.3                 # Readability metrics (Flesch, Gunning Fog, SMOG)
nltk==3.8.1                     # Natural language toolkit (tokenization, POS)
textblob==0.18.0                # Sentiment analysis, subjectivity scoring

# Anti-AI Detection - REQUIRED
language-tool-python==2.7.1    # Grammar checking, style violation detection

# =============================================================================
# STATISTICS & ANALYTICS - FOR SCORING ALGORITHMS (NO LLM INFERENCE)
# =============================================================================
scipy==1.11.4                   # Statistical tests (A/B testing, z-tests)
statsmodels==0.14.1             # Proportion tests, confidence intervals
numpy==1.26.3                   # Numerical computing (already likely installed)
scikit-learn==1.4.0             # TF-IDF vectorization, cosine similarity (NO LLM)

# =============================================================================
# DATA QUALITY & ENRICHMENT
# =============================================================================
great-expectations==0.18.8     # Data validation and quality testing
pyjanitor==0.27.0              # DataFrame cleaning utilities

# Monitoring & Logging
structlog==24.1.0
sentry-sdk==1.39.2

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3

# =============================================================================
# CREWAI - MULTI-AGENT ORCHESTRATION
# =============================================================================
crewai>=0.28.0                  # Role-based multi-agent framework
crewai-tools>=0.1.0             # CrewAI tool integrations

# =============================================================================
# LLa-MARKETING - DOMAIN-SPECIFIC LLM (OPTIONAL)
# =============================================================================
# Enable by setting CONTENT_LLM_PROVIDER=llamarketing
transformers>=4.36.0            # Hugging Face transformers
accelerate>=0.25.0              # Model loading optimization
bitsandbytes>=0.41.0            # 8-bit quantization (reduces to ~8GB RAM)
